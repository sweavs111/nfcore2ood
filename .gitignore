# macOS / OS noise
.DS_Store
._*
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
.coverage.*
htmlcov/
.venv/
venv/
env/
ENV/

# Build / packaging artifacts
build/
dist/
*.egg

# Logs and temp files
*.log
*.tmp
*.bak
*.orig
*.rej

# Direnv / local shell wrappers
.envrc

# Site-specific configuration. Copy nf2ood.env.example to nf2ood.env
# and fill in your site values; the real file should never be committed.
nf2ood.env
nf2ood.env.*
!nf2ood.env.example

# Local generated output trees (typical nf2ood --output targets and
# pipeline install roots used during local development).
ood/
modules/
generated-apps/
.deploy-staging/

# Local Nextflow dist-cache bind mount and nf-core-download test shim
# (see download_nfcore_pipeline.sh) -- machine-local, never committed.
.nf-dist-cache/
.nf-json-shim/
